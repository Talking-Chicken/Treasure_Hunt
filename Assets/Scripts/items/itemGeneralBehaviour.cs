using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class itemGeneralBehaviour : MonoBehaviour
{
    public bool inPackage;
    public bool onHand;

    // Start is called before the first frame update
    void Start()
    {
        inPackage = false;
        onHand = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void showInPackage()
    {
        return;
    }

}
